{
  "name": "autotests-run-button",
  "version": "1.0.0",
  "private": true,
  "description": "Public run-button + Allure report viewer, backed by the GitHub Actions workflow in the main repo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "adm-zip": "^0.6.0"
  }
}
